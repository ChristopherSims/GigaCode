Agent does not store code in context, only the location of the buffer files

add a second shader that clusters or groups similar code regions automatically


If code base is too large, add a warning reponse to agent not to use tool


#sudo apt install nvidia-driver-535 vulkan-tools libvulkan1   