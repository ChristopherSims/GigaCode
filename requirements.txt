# Core dependencies for GigaCode Vulkan embedding tool
numpy>=1.24.0
sentence-transformers>=2.2.0
torch>=2.0.0
vulkan>=1.3.0

# Optional but recommended
tiktoken>=0.5.0
tree-sitter>=0.20.0

# Dev / test
pytest>=7.0.0
